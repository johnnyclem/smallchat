{
  "mcpServers": {
    "webhook-channel": {
      "command": "npx",
      "args": [
        "smallchat",
        "channel",
        "--name", "webhook",
        "--two-way",
        "--http-bridge",
        "--http-bridge-port", "3002",
        "--http-bridge-secret", "change-me-in-production",
        "--instructions", "This channel receives webhook notifications. Summarize events concisely and respond to user questions about them."
      ]
    }
  }
}
